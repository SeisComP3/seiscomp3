SET(SCDISPATCH_TARGET scdispatch)
	
SET( 
	SCDISPATCH_HEADERS
)

SET(
    SCDISPATCH_SOURCES
	main.cpp
)

SC_ADD_EXECUTABLE(
	SCDISPATCH 
	${SCDISPATCH_TARGET}
)

SC_LINK_LIBRARIES_INTERNAL(
	${SCDISPATCH_TARGET} 
	client 
)

SC_LINK_LIBRARIES(
	${SCDISPATCH_TARGET}
	${Boost_filesystem_LIBRARY}
)

FILE(GLOB descs "${CMAKE_CURRENT_SOURCE_DIR}/descriptions/*.xml")
INSTALL(FILES ${descs} DESTINATION ${SC3_PACKAGE_APP_DESC_DIR})
