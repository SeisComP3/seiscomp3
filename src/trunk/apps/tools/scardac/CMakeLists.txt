SET(SCARDAC_TARGET scardac)
SET(SCARDAC_SOURCES main.cpp scardac.cpp)

INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})

SC_ADD_EXECUTABLE(SCARDAC ${SCARDAC_TARGET})
SC_LINK_LIBRARIES_INTERNAL(${SCARDAC_TARGET} client)
SC_INSTALL_INIT(${SCARDAC_TARGET} ../../templates/initd.py)

FILE(GLOB descs "${CMAKE_CURRENT_SOURCE_DIR}/descriptions/*.xml")
INSTALL(FILES ${descs} DESTINATION ${SC3_PACKAGE_APP_DESC_DIR})
