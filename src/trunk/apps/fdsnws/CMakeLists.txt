IF(CMAKE_MAJOR_VERSION GREATER 2)
	SUBDIRS(test)
ENDIF(CMAKE_MAJOR_VERSION GREATER 2)

SC_ADD_PYTHON_PROG(fdsnws)
SC_ADD_PYTHON_PROG(fdsnws_fetch)
SC_ADD_PYTHON_PROG(fdsnws2sds)
SC_INSTALL_INIT(fdsnws initd.py)

# python library files
SET(PYTHON_PACKAGE_PATH ${SC3_PACKAGE_PYTHON_LIB_DIR}/seiscomp3)
INSTALL(DIRECTORY fdsnws DESTINATION ${PYTHON_PACKAGE_PATH}
	PATTERN ".svn" EXCLUDE
	PATTERN "CMakeLists.txt" EXCLUDE
	PATTERN "*~" EXCLUDE
	PATTERN "*.pyc" EXCLUDE)

# data files
SET(FDSNWS_SHARE_PATH ${SC3_PACKAGE_SHARE_DIR}/fdsnws)
INSTALL(DIRECTORY share/ DESTINATION ${FDSNWS_SHARE_PATH} USE_SOURCE_PERMISSIONS
	PATTERN ".svn" EXCLUDE
	PATTERN "*~" EXCLUDE)

# descriptions
FILE(GLOB descs "${CMAKE_CURRENT_SOURCE_DIR}/descriptions/*.xml")
INSTALL(FILES ${descs} DESTINATION ${SC3_PACKAGE_APP_DESC_DIR})
