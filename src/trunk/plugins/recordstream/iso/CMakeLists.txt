FIND_PACKAGE(Cdio)

IF (CDIO_FOUND)
	SET(
		ISO_SOURCES
			isoarchive.cpp
			isofile.cpp
	)

	SC_ADD_PLUGIN_LIBRARY(ISO iso "")
	INCLUDE_DIRECTORIES(${CDIO_INCLUDE_DIR})
	SC_LINK_LIBRARIES(iso ${CDIO_LIBRARIES})
	SC_LINK_LIBRARIES_INTERNAL(iso core)
ENDIF (CDIO_FOUND)
