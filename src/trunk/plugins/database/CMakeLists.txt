IF (SC_TRUNK_DB_MYSQL AND MYSQL_FOUND)
    SET(INTERFACES ${INTERFACES} mysql)
ENDIF (SC_TRUNK_DB_MYSQL AND MYSQL_FOUND)

IF (SC_TRUNK_DB_SQLITE3 AND SQLITE3_FOUND)
    SET(INTERFACES ${INTERFACES} sqlite)
ENDIF (SC_TRUNK_DB_SQLITE3 AND SQLITE3_FOUND)

IF (SC_TRUNK_DB_POSTGRESQL AND POSTGRESQL_FOUND)
    SET(INTERFACES ${INTERFACES} postgresql)
ENDIF (SC_TRUNK_DB_POSTGRESQL AND POSTGRESQL_FOUND)

IF (INTERFACES)
    SUBDIRS(${INTERFACES})
ENDIF (INTERFACES)
