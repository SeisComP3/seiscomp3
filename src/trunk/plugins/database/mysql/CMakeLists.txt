SET(
	DATABASE_MYSQL_SOURCES
		mysqldatabaseinterface.cpp
)

SET(
	DATABASE_MYSQL_HEADERS
		mysqldatabaseinterface.h
)

INCLUDE_DIRECTORIES(${MYSQL_INCLUDE_DIR})
INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIRS})

SC_ADD_PLUGIN_LIBRARY(DATABASE_MYSQL dbmysql "")

SC_LINK_LIBRARIES(dbmysql ${MYSQL_LIBRARIES})
SC_LINK_LIBRARIES_INTERNAL(dbmysql core)
