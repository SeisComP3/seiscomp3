SET(MSEEDSCAN_PLUGIN_SOURCES
	mseedscan.c
	seedutil.c
	rbtree.c
	stack.c
)

INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(..)
INCLUDE_DIRECTORIES(${SLPLUGINSDIR})

ADD_EXECUTABLE(mseedscan_plugin ${MSEEDSCAN_PLUGIN_SOURCES})
TARGET_LINK_LIBRARIES(mseedscan_plugin slplugin)

INSTALL(TARGETS mseedscan_plugin RUNTIME DESTINATION ${SEEDLINK_PLUGIN_OUTPUT_DIR})
