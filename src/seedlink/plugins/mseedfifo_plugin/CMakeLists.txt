SET(MSEEDFIFO_PLUGIN_SOURCES
	mseedfifo_plugin.c
	../plugin.c
)

INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(..)
INCLUDE_DIRECTORIES(../../libs/seiscomp/plugin)
INCLUDE_DIRECTORIES(../../libs/3rd-party/libslink)


SET(OPTIONS -DSYSLOG_FACILITY=LOG_LOCAL0)

ADD_DEFINITIONS(${OPTIONS})

ADD_EXECUTABLE(mseedfifo_plugin ${MSEEDFIFO_PLUGIN_SOURCES})
TARGET_LINK_LIBRARIES(mseedfifo_plugin slink)

INSTALL(TARGETS mseedfifo_plugin RUNTIME DESTINATION ${SEEDLINK_PLUGIN_OUTPUT_DIR})
