SET(ECHOPROPLUGIN_SOURCES
	echopro_plugin.c
	../plugin.c
)

INCLUDE_DIRECTORIES(.)
INCLUDE_DIRECTORIES(..)

ADD_EXECUTABLE(echopro_plugin ${ECHOPROPLUGIN_SOURCES})
TARGET_LINK_LIBRARIES(echopro_plugin m)

INSTALL(TARGETS echopro_plugin RUNTIME DESTINATION ${SEEDLINK_PLUGIN_OUTPUT_DIR})
