IF ( ${PYTHON_VERSION} STREQUAL "2.3" )
	INSTALL(
		FILES decimal.py
		DESTINATION ${SC3_PACKAGE_PYTHON_LIB_DIR}
		USE_SOURCE_PERMISSIONS
	)
ENDIF ( ${PYTHON_VERSION} STREQUAL "2.3" )

SUBDIRS(seiscomp3)
