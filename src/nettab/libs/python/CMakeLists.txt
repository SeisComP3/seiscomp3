IF(SC_GLOBAL_PYTHON_WRAPPER)

SET(PYTHON_PACKAGE_PATH ${SC3_PACKAGE_PYTHON_LIB_DIR})

INSTALL(DIRECTORY 
	nettab
	DESTINATION ${PYTHON_PACKAGE_PATH}
        USE_SOURCE_PERMISSIONS
	PATTERN "*~" EXCLUDE
	PATTERN "CMakeLists.txt*" EXCLUDE
	PATTERN ".svn" EXCLUDE
)
ENDIF(SC_GLOBAL_PYTHON_WRAPPER)
