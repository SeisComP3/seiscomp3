SET(PACKAGE_NAME PRELININARYORIGINPLUGIN)
SET(LIB_NAME mm_preliminaryorigin)

SET(
	${PACKAGE_NAME}_SOURCES
		plugin.cpp
		mainwidget.cpp
)

SET(
	${PACKAGE_NAME}_HEADERS
)

SET(
	${PACKAGE_NAME}_MOC_HEADERS
		plugin.h
		mainwidget.h
)

SET(
	${PACKAGE_NAME}_UI
)

SC_ADD_GUI_LIBRARY_CUSTOM_INSTALL(${PACKAGE_NAME} ${LIB_NAME})
SC_PLUGIN_INSTALL(${LIB_NAME} scmm)
SC_LIB_LINK_LIBRARIES_INTERNAL(${LIB_NAME} qt4)
